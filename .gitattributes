# Shell scripts and container files must stay LF so they run in Linux containers / WSL.
scripts/dev text eol=lf
gradlew text eol=lf
*.sh text eol=lf
Dockerfile text eol=lf
*.Dockerfile text eol=lf
docker/**/Dockerfile text eol=lf
*.yml text eol=lf

# Story 0083: the committed pre-port deck database fixture is binary; never line-ending-converted.
*.db binary
