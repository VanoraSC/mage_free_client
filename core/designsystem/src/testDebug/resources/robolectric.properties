# Pin Robolectric to a shipped android-all image (compileSdk is 36, which Robolectric 4.14 predates).
# The Compose tests here render only design-system components, which use nothing level-specific.
sdk=34
